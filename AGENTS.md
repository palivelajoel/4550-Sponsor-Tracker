# Working Agreement

- Always push and ship changes (commit and push to main when work is complete; Vercel auto-deploys on push).
- If a single prompt contains multiple tasks, always run multiple agents in parallel — one agent per task.
- Never commit secrets or environment variables.